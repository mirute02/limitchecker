# セキュリティ / 個人情報 監査

実施日: 2026-09-20 / 対象: 履歴全体（public 化で全コミットが公開されるため）

## 結果

| 検査 | 結果 |
| --- | --- |
| コミットの作成者 | noreply アドレスのみ。勤務先メールは履歴に存在しない |
| `.env` / 署名鍵 / `google-services.json` / `local.properties` | 一度もコミットされていない |
| 履歴中の全ブロブに実トークン・API キー・秘密鍵 | 0 件 |
| 履歴中の端末の絶対パス | 0 件 |
| 履歴中のメールアドレス（noreply / example を除く） | 0 件 |
| 履歴中の Tailscale アドレス | 0 件 |
| ホスト名の自動取得（`gethostname` / `Build.SERIAL` 等） | 0 件。ユーザが設定したラベルのみ使用 |
| APK 内の実トークン | 含まれない |
| APK 内の端末の絶対パス | 含まれない |
| `allowBackup` | `false`（端末バックアップから除外） |
| 平文 HTTP | `networkSecurityConfig` で localhost と `*.ts.net` に限定 |
| トークンのログ出力 | 経路なし。Android 側にログ出力自体が存在しない |
| 例外メッセージの画面表示 | なし。固定文言に置き換えている |
| hub の認証 | `hmac.compare_digest`（定数時間比較） |
| hub の待ち受け | 既定 127.0.0.1。`0.0.0.0` と空文字は起動拒否 |
| hub のトークン長 | 16文字未満は起動拒否。既定トークンなし |
| hub のボディ上限 | 64KB |
| `.env.example` の実値 | なし |

検出器そのものが動作していることを、必ず存在する文字列で毎回確認したうえで実施した。
無出力を「検出なし」と早合点しないため。

## 対処した項目

**WorkManager の診断用レシーバを除去**。`androidx.work.impl.diagnostics.DiagnosticsReceiver`
は `exported=true` で追加される。`DUMP` 権限で守られており実害は考えにくいが、
本アプリでは使わないため公開面から外した（`tools:node="remove"`）。

## 残っている公開コンポーネント（いずれも妥当）

| コンポーネント | 理由 |
| --- | --- |
| `SettingsActivity` | ランチャーとウィジェット設定画面。外部から起動されても、トークンは表示しない。受け取るのは `EXTRA_APPWIDGET_ID` のみ |
| `SystemJobService`（WorkManager） | JobScheduler がバインドするため必須。`BIND_JOB_SERVICE` 権限で保護 |
| `ProfileInstallReceiver` | 起動性能の改善用。`DUMP` 権限で保護 |

## 未対応

**LICENSE がない。** 公開リポジトリでライセンス表記がない場合、既定では
全権利が作者に留保され、第三者は利用も改変もできない。公開前に決める必要がある。

## 次回以降の確認項目

- 依存を追加したら、ビルド後の APK で権限と公開コンポーネントを実測する
  （マニフェストを読むだけでは足りない。実際に見落とした）
- public 化の直前に、本監査をもう一度実施する

## 追加監査（公開後、検査手法の見直し）

初回監査のパターンに **PCRE 構文（`\s` `\b` 否定先読み）が混ざっており、
POSIX 拡張正規表現では解釈されず、一部の検査が実際には動いていなかった。**
`\s` を使った検査は 0 件、`[[:space:]]` に直すと 34 件ヒットした。
Tailscale アドレスの検査も `\b` を使っていたため同様に無効だった。

**検出器そのものの動作を、必ず存在する文字列で毎回確かめる**手順に改めて、
公開済みリポジトリを新規 clone して検査し直した。

### 検査範囲

| 対象 | 範囲 |
| --- | --- |
| ブロブ | 111 個（全25リビジョン、到達不能オブジェクトを含む） |
| ファイル形式 | 検査時点では全ファイルがテキスト（**下記の訂正を参照**） |
| コミットメッセージ | git grep はファイルしか見ないため別途走査 |
| リモートの実体 | GitHub API で直接確認 |

### 結果

- **秘密に関わる代入を全件列挙して目視**（`token` `password` `secret` `api_key`
  `credential` `private_key` `bearer`）。すべて変数・引数・空文字・キー名・
  生成コマンドで、値の直書きは 0 件
- **エントロピー検査**（4.0 bit/文字以上・24文字以上）で 9 件ヒット。
  すべて読める形のパスや識別子。ランダムな秘密は 0 件
- メールアドレス: ファイル内 0 件。コミットメタデータに noreply 2種のみ
- Tailscale アドレス: 0 件
- `.env` / `local.properties` / `google-services.json`: リモートに存在しないことを
  GitHub API で確認

### 教訓

`grep -E` で `\s` `\b` `\d` `(?!)` は使わない。POSIX 文字クラスを使う。
そして**検出器が動くことを確認していない「0 件」は、結果ではない。**

## 訂正（独立監査の指摘による）

上の「全ファイルがテキスト。バイナリによる検査の死角はない」という記述は、
**公開時点で既に事実と異なっていた。**

検査そのものは clone した時点の内容に対して正しかったが、その結果を記録した
コミット（3da8279）を `git add -A` で作った際に、**まだ組み込んでいなかった
アイコンの PNG 15枚を巻き込んでいた**。つまり「バイナリはない」と書いた
コミット自身がバイナリを追加している。

PNG 15枚は独立監査で改めて検査され、`IHDR` / `IDAT` / `IEND` のみで
テキストチャンク（`tEXt` / `zTXt` / `iTXt`）も IEND 以降の付加データもなく、
埋め込みの痕跡はないことが確認されている。結論は変わらない。

### 教訓

- **`git add -A` は作業中の未追跡ファイルまで巻き込む。** 監査のように
  「その時点の状態」を記録するコミットでは、対象を明示して add する
- 検査結果を書いた文章は、**その文章をコミットした時点でも正しいか**を確かめる。
  検査時点で正しかったことは、公開時点で正しいことを意味しない

## `\b` と `\s` の扱いについて（決着）

対照試験で確認した挙動:

| | `\b` | `\s` |
| --- | --- | --- |
| `git grep -E` | **効かない**（127.0.0.1 の検出が 0 件 vs POSIX 版 8 件） | 効かない |
| スタンドアロンの `grep -E`（本環境は ugrep） | 効く | — |

つまり初回監査の Tailscale 検査（`git grep` + `\b`）は無効だった。
POSIX 構文で再検査し、0 件であることを確認済み。

`.githooks/pre-commit` の Tailscale 検査も `\b` を使っていた。本環境の grep では
動作していたが、macOS / BSD の grep では保証がないため POSIX 構文に直した。
公開リポジトリとして、他の環境でフックが黙って無効になる状態は避ける。
修正後、4種の検出器すべてが発火することを対照試験で確認した。

## 独立監査（別モデルによる検証）

前任（本セッション）の検査が一度壊れていたため、別のエージェントに
手順も結論も渡さずに再検証させた。

**秘密情報の監査結果**: 27リビジョン / 131 ブロブ / 200 ツリーを対象に、
既知プレフィックス・PEM 秘密鍵・JWT・URL 埋め込み認証情報・エントロピー解析・
第2の正規表現エンジンによる二重確認を実施。**秘密情報 0 件、個人情報 0 件。**
12種のカナリア文字列で検出器の発火を確認したうえでの結論。

到達不能オブジェクト 0、リモートのブランチは main のみ、タグ・リリース・
フォーク・Wiki なし、force-push による孤立コミットの痕跡なしも確認された。

### 独立監査が挙げた検査の限界

- サーバ側にのみ残る孤立オブジェクト（clone に含まれず SHA なしでは取得不能）
- 低エントロピーで人間が読める形の秘密（辞書語のパスワード等）
- 分割・エンコード・難読化された秘密
- PNG の画素へのステガノグラフィ（チャンク構造までは確認済み）
- GitHub 上の非 git 資産（Issues / Actions ログ / Gist 等）
- 監査後に push された内容
